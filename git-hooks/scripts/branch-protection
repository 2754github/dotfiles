#!/bin/bash
set -eu
trap '[[ $? -eq 0 ]] && echo "✅$0"' EXIT

current=$(git branch --show-current)
branches=(develop main master)
if [[ " ${branches[@]} " =~ " $current " ]]; then
  echo "❌do not push $current"
  exit 1
fi
